{
  "name": "hello-node-app",
  "version": "1.0.0",
  "description": "Aplikasi Demo Deployment via SSH",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}